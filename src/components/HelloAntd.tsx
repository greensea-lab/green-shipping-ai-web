import React from 'react';
import { Button } from 'antd';

const HelloAntd: React.FC = () => {
  return (
    <div style={{ margin: '24px 0' }}>
      <Button type="primary">Ant Design 버튼</Button>
    </div>
  );
};

export default HelloAntd;
